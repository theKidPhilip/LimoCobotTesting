Build + test commands (exact)
cd ~/development/ros/ros2_ws
colcon build --packages-select wall_follower blazerbot_bringup --symlink-install
source install/setup.zsh

# Verify executables exist
ros2 pkg executables wall_follower | grep wall_follower_node
ros2 pkg executables blazerbot_bringup | grep arm_then_run_wall_follower

# Run bringup
ros2 launch blazerbot_bringup robot_bringup.launch.py

Press S → wall follower starts.

Quick “did it start?” checks (use another terminal)
ros2 node list | grep wall_follower
ros2 topic echo /cmd_vel --once