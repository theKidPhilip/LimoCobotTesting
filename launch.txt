bash
ros2 service call /slam_toolbox/dynamic_map nav_msgs/srv/GetMap {}
If that returns a map, SLAM has built it internally but just isn't publishing to the topic. Also check:
bash
ros2 topic echo /map_updates --once